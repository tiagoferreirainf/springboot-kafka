server:
  port: 8082

spring:
  profiles:
    active: ${RUN_MODE:local}

tf:
  springboot:
    kafka:
      bootstrapServer: "localhost:9093"
      groupId: "group-test"
      consumer:
        enabled: true